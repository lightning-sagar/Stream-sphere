
<style>
    body {
      font-family: 'Playpen Sans', cursive !important;
      font-family: 'Plus Jakarta Sans', sans-serif !important;
    }
  
    .navbar :hover{
      color: #5bc0de;
    }
    .search-btn {
      background-color: #fe424d;
      color: #fff;
      border-radius: 25px;
      padding: 0 1rem;
    }
  
    .search-btn:hover {
      background-color: #ff2e33;
      color: #fff;
    }
  
    .search-btn i {
      display: inline;
      margin-right: 0.5rem;
    }
  
    .navbar .search-btn {
      background-color: #fe424d;
      color: #fff;
      border-radius: 25px;
      padding: 0 1rem;
  }
  
  .navbar .search-btn:hover {
      background-color: #ff2e33;
      color: #fff;
  }
  
  /* Add similar specificity for other navbar elements */
  .navbar .search-inp {
      border-radius: 25px;
      padding: 0.5rem 3rem 0.5rem 3rem;
      font-size: 1rem;
  }
    .search-inp {
      border-radius: 25px;
      padding: 0.5rem 3rem 0.5rem 3rem;
      font-size: 1rem;
    }
  
    .search-div {
      margin-right: 17rem;
    }
  
  @media (max-width: 1000px) {
    .navbar .add-notes-link-mobile {
      order: 1;
      margin-bottom: 1rem;
  }
      .search-div {
        margin-right: 0;
      }
      .navbar-collapse {
        position: absolute;
        top: 56px; /* Adjust this value based on your design */
        left: 0;
        right: 0;
        background-color: #fff; /* Set a background color for the collapsed menu */
        z-index: 1000;
        box-shadow: 0 4px 2px -2px #333; /* Add a box shadow for a separation effect */
      }
      .login-btns {
        display: flex;
        flex-direction: column;
        align-items: flex-end;
        margin-top: 1rem;
      }
  
      .navbar-nav {
        flex-direction: column;
        align-items: flex-end;
      }
  
      .navbar-toggler {
        order: -1;
      }
  
      .navbar-nav.login-btns {
        order: 2;
        margin-top: 0;
      }
  
      .navbar-nav.login-btns .nav-link {
        margin-bottom: 0.5rem;
      }
  
      .search-btn {
        padding: 0.5rem 1rem;
        display: none;
  
      }
  
      .search-inp {
        padding: 0.5rem;
        display: none;
      }
    }
  
    .delete-forms {
      display: flex;
      align-items: center;
      margin-right: 1rem;
    }
  
    .delete-forms p {
      margin-right: 0.5rem;
      cursor: pointer;
      font-weight: bold;
    }
  
    .delete-forms i {
      font-size: 1.5rem;
      cursor: pointer;
      opacity: 0.7;
    }
  
    .del {
      color: #d9534f;   
    }
  
    .nav-link {
      font-weight: bold;
      margin-right: 1rem;
      color: #33f76e;
    }
  
    .add {
      color: #5bc0de;  
    }
  
    .logout-link {
      color: #d9534f; 
    }
    .navbar{
      background: #333;
      color: #fff!important;
    }
    .custom-darkmode{
      cursor: pointer;
      align-items: center;
      display: flex;
    }
    .sixe{
      font-size: 10px;
    }
  </style>
  <nav class="navbar navbar-expand-md navbar-light bg-body-light sticky-top border-bottom">
    <% if (!currentUser ) { %>
    <div class="container-fluid">
      <a class="navbar-brand" href="/user/${req.user._id}"><i class="fa-regular fa-compass"></i></a>
      <button class="navbar-toggler " type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon "></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
          <li class="nav-item">
            <a class="nav-link" href="/user/${req.user._id}">Home</a>
          </li>
        </ul>
        <div class="navbar-nav  login-btns" id="navbarNav">
          <a class="nav-link add-notes-link" href="/user/${req.user._id}/upload"> <i class="fa-solid fa-plus add"></i> &nbsp; &nbsp;  </a>
        </div>  
  
        <div class="navbar-nav ms-auto login-btns" id="navbarNav">
          <% if(!currentUser){ %>
            <a class="nav-link" aria-current="page" href="/signup">Sign-up</a>
            <a class="nav-link" href="/login">Login</a>
          <% } %>
          <% if(currentUser){ %>
            <a class="nav-link logout-link" href="/logout">Logout</a>
          <% } %>
        </div>
      </div>
    </div>
    <% }else{ %>
      <div class="container-fluid">
      
      <a class="navbar-brand " href="/"><i class="fa-regular fa-compass"></i></a>
      <button class="navbar-toggler " type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon "></span>
      </button>
      <button onclick="onChangeBtn()" class="custom-darkmode"></button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
          <li class="nav-item">
            <a class="nav-link" href="/user/<%= req.user._id %>">Home</a>
          </li>
        </ul>
        <div class="navbar-nav  login-btns">
          <a class="nav-link add-notes-link" href="/user/<%= req.user._id %>/upload"> &nbsp; &nbsp;Upload video </a>
        </div>  
        <form id="searchForm" class="d-flex ms-auto" role="search" action="/user/<%= req.user._id %>/search" method="GET">
          <input class="form-control sixe me-2 ql-size-small search-inp" type="search" id="searchInput" name="title" placeholder="Date(MM-DD) or title" aria-label="Search">
          <button class="btn search-btn" type="submit"><i class="fa-solid fa-magnifying-glass"></i>Search</button>
        </form>
        <div class="navbar-nav ms-auto login-btns">
          <% if(!currentUser){ %>
            <a class="nav-link" aria-current="page" href="/signup">Sign-up</a>
            <a class="nav-link" href="/login">Login</a>
          <% } %>
          <% if(currentUser){ %>
            <a class="nav-link logout-link" href="/logout">Logout</a>
          <% } %>
        </div>
      </div>
    </div>
    <script>
      if(currentUser){
        let searchForm = document.getElementById('searchForm');
        searchForm.addEventListener('submit', function (event) {
          event.preventDefault(); 
          const inputValue = document.getElementById('searchInput').value;
          window.location.href = `/user/<%= req.user._id %>/search/${inputValue}`;
        });
      }
    </script>
    <% } %>
  </nav>
  